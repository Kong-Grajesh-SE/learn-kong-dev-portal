# API Changelog

## 2026-05-28 - Bookstore API v1.0.0

- Initial release
- **Books:** `GET /books`, `POST /books`, `GET /books/{bookId}`, `PUT /books/{bookId}`, `DELETE /books/{bookId}`
- **Authors:** `GET /authors`, `GET /authors/{authorId}`
- **Reviews:** `GET /reviews`, `POST /reviews`
- Authentication: API key in `apikey` header
- Backend: httpbun.com echo service (for development/testing)

---

*Subscribe to portal notifications to be alerted when new versions are published.*
